// src/Main.java
import view.CLI;

public class Main {
    public static void main(String[] args) {
        // Initialize the CLI (Command Line Interface) and start the main menu
        CLI cli = new CLI();
        cli.mainMenu();
    }
}
